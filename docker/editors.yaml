editors:
  opencode:
    install: "curl -fsSL https://opencode.ai/install | bash -s -- --version $VERSION && mv /root/.opencode/bin/opencode /usr/local/bin/opencode && rm -rf /root/.opencode"
    cmd: "opencode"
  claude:
    install: "curl -fsSL https://claude.ai/install.sh | bash"
    cmd: "claude"
  aider:
    install: "curl -LsSf https://aider.chat/install.sh | sh"
    cmd: "aider"
  copilot:
    install: "export VERSION=$VERSION && curl -fsSL https://gh.io/copilot-install | bash"
    cmd: "copilot"
  vibe:
    install: "curl -LsSf https://mistral.ai/vibe/install.sh | bash"
    cmd: "vibe"
  goose:
    install: "export GOOSE_VERSION=$VERSION && curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | bash || true"
    cmd: "goose"
  codex:
    install: "npm i -g @openai/codex"
    cmd: "codex"
  gemini:
    install: "npm install -g @google/gemini-cli"
    cmd: "gemini"
  cursor:
    install: "curl https://cursor.com/install -fsS | bash"
    cmd: "cursor"
